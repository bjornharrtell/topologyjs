
class Topology
    constructor: ->
        @faces = {}
        @edges = {}
        @nodes = {}
        
        # create face 0
        
    addPolygon: (polygon) ->
        
        # create face from polygon
    
    addLineString: (linestring) ->
        
        # create edge from linestring
        
    addPoint: (point) ->
        
        # create node from point
